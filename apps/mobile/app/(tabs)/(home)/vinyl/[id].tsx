// The vinyl detail screen lives in src/screens so every tab stack that can open a record (here and
// Favorites) shares one implementation. This route file just mounts it within the Home stack.
export { default } from '../../../../src/screens/vinyl-detail';
