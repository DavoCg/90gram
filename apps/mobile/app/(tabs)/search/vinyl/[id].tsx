// Same shared vinyl detail screen as the Home and Favorites stacks, mounted here so opening a record
// from Search pushes ON TOP of the search list (and keeps the Search tab active) instead of jumping
// to Home. See src/screens/vinyl-detail.tsx.
export { default } from '../../../../src/screens/vinyl-detail';
