// Same shared vinyl detail screen as the Home stack, mounted here so opening a record from
// Favorites pushes ON TOP of the favorites list (and keeps the Favorites tab active) instead of
// jumping to Home. See src/screens/vinyl-detail.tsx.
export { default } from '../../../../src/screens/vinyl-detail';
