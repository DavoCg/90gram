// Same shared vinyl detail screen as the other tabs, mounted here so opening a record from a profile
// or a collection pushes ON TOP of the profile stack (and keeps the Profile tab active). See
// src/screens/vinyl-detail.tsx.
export { default } from '../../../../src/screens/vinyl-detail';
